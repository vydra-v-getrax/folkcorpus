проблемы

- не перезапускается с латинскими тегами база - ошибка записи, тогда надо менять либо в индексе либо в базе чинить
- ищет теги по всем возможным разборам слова, а надо только в нулевом. forms.tags.0, как советуют в интернете, не работает 
- как написать запрос, чтобы в группе было несколько условий? запрос, чтобы найти (s1 ИЛИ s2) И (+r ИЛИ -r)? С группировкой у меня не получилось


на будущее
-- надо генерировать страницы под кажды текст и добавлять ссылку на полный текст в новом окне со всей инфой
-- надо сохранять спаны для всех сущностей и подсвечивать их на странице выдачи
